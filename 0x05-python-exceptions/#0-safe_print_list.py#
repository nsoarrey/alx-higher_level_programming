#!/usr/bin/python3

def safe_print_list(my_list=[], x=0):
    cout == 0
    for i in range(1, x):
        try:
            print(f"{my_list[i]}", end = "")
    except IndexError:
        break
    print()
